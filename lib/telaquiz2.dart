import 'package:flutter/material.dart';

class Telaquiz2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Tela 2'),
      ),
    );
  }
}